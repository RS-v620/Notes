It is a stateless, light-weight `remote procedure call` protocol that uses JSON as its data format. It defines a simple and flexible message structure for invoking methods ( a.k.a functions ) on a remote server, supporting ==HTTP==, ==WebSockets== and ==TCP==.

JSON-RPC 2.0 messages are JSON objects with specific, standardized fields ( so perhaps like XML but more limited ?? )