---
def-type: atomic
aliases: 
- hasing function
---
A hash function is any function that can be used to map data of arbitrary size to fixed-size values.
Though there are some hash functions that support variable-length output.[1] The values returned by a hash function are called hash values, hash codes, (hash/message) digests,[2] or simply hashes.